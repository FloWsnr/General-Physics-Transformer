[project]
name = "gphyt"
version = "0.1.0"
description = "Towards a General Physics Transformer"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools]
packages = ["gphyt"]

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or "GPHYT Blog" }}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            line-height: 1.6;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            color: #333;
        }
        h1, h2, h3 {
            color: #2c3e50;
        }
        header h1 {
            text-align: center;
        }
        a {
            color: #3498db;
            text-decoration: none;
            transition: color 0.2s ease;
        }
        a:hover {
            color: #2980b9;
            text-decoration: underline;
        }
        a:visited {
            color: #8e44ad;
        }
        code {
            background: #f4f4f4;
            padding: 2px 4px;
            border-radius: 3px;
        }
        pre {
            background: #f4f4f4;
            padding: 15px;
            border-radius: 5px;
            overflow-x: auto;
        }
        .gif-grid-2x2 {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
            margin: 20px 0;
        }
        .gif-grid-3x3 {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 15px;
            margin: 20px 0;
        }
        .gif-grid-3x3-nospace {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 0px;
            margin: 0px 0;
        }
        .gif-grid-4x2-nospace {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            grid-template-rows: repeat(2, 1fr);
            gap: 0px;
            margin: 0px 0;
        }
        .gif-grid-3x2-nospace {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            grid-template-rows: repeat(2, 1fr);
            gap: 0px;
            margin: 0px 0;
        }

        .gif-grid-2x2 img,
        .gif-grid-3x3 img {
            width: 100%;
            height: auto;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        .gif-grid-3x3-nospace img,
        .gif-grid-4x2-nospace img,
        .gif-grid-3x2-nospace img {
            width: 100%;
            height: auto;
            border-radius: 0;
            box-shadow: none;
            transition: transform 0.2s ease;
        }

        .gif-grid-3x3-nospace img:hover,
        .gif-grid-4x2-nospace img:hover,
        .gif-grid-3x2-nospace img:hover,
        .gif-grid-3x3 img:hover,
        .gif-grid-2x2 img:hover {
            transform: scale(1.02);
        }

        /* Zoom overlay styles */
        .zoom-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            z-index: 1000;
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            transition: opacity 0.3s ease;
            cursor: pointer;
        }

        .zoom-overlay.active {
            opacity: 1;
        }

        .zoomed-container {
            position: relative;
            width: 98vw;
            height: 98vh;
            cursor: default;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .zoomed-image {
            width: 98vw;
            height: 85vh;
            object-fit: contain;
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
        }

        .zoom-close {
            position: absolute;
            top: -15px;
            right: -15px;
            width: 40px;
            height: 40px;
            background: rgba(255, 255, 255, 0.9);
            border: none;
            border-radius: 50%;
            font-size: 24px;
            font-weight: bold;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: background 0.2s ease;
        }

        .zoom-close:hover {
            background: rgba(255, 255, 255, 1);
        }

        .zoom-caption {
            color: white;
            font-size: 20px;
            font-weight: 500;
            text-align: center;
            margin-top: 15px;
            padding: 12px 20px;
            background: rgba(0, 0, 0, 0.7);
            border-radius: 6px;
            max-width: 80%;
            line-height: 1.4;
        }

        .arch-image {
            width: 100%;
            height: auto;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            margin: 20px 0;
        }

        .authors {
            text-align: center;
            margin: 20px 0 40px 0;
            font-size: 1.1em;
            color: #666;
            font-style: italic;
        }
    </style>

    <!-- MathJax Configuration -->
    <script>
    MathJax = {
        tex: {
            inlineMath: [['$', '$'], ['\\(', '\\)']],
            displayMath: [['$$', '$$'], ['\\[', '\\]']],
            processEscapes: true,
            processEnvironments: true
        },
        options: {
            skipHtmlTags: ['script', 'noscript', 'style', 'textarea', 'pre']
        }
    };
    </script>
    <script src="https://polyfill.io/v3/polyfill.min.js?features=es6"></script>
    <script id="MathJax-script" async src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>
</head>
<body>
    <header>
        <h1>{{ title or "GPHYT Project Blog" }}</h1>
    </header>

    <main>
        {{ content | safe }}
    </main>

    <footer>
        <p>&copy; Florian Wiesner</p>
    </footer>

    <script src="assets/gif-zoom.js"></script>
</body>
</html>